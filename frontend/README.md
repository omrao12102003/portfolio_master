# Frontend

Vite + React + TypeScript shell for Portfolio Master.

This is not the research dashboard. It exists so later stages can add pages without
renaming the project.

```bash
npm install
npm run dev      # http://127.0.0.1:5173
npm run build
npm run preview
```
